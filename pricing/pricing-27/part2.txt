on{width:28px;height:28px}.selected-plan-details .summary-icon img{width:17px;height:17px}}
@media(max-width:900px){
  .selected-plan{display:flex;flex-direction:column;}
  .selected-plan-top{order:1;}
  .pricing-plan-accent{order:2;}
  .selected-plan-details{order:5;}
  .pricing-plan-sign-up{order:6;}
  .reassurance{order:7;}
}
.selected-plan{
  display:flex;
  flex-direction:column;
  max-height:calc(100vh - 48px);
}
.selected-plan-body{
  min-height:0;
  overflow-y:auto;
  -webkit-overflow-scrolling:touch;
  padding-bottom:8px;
}
.selected-plan-footer{
  flex:0 0 auto;
  position:sticky;
  bottom:0;
  z-index:5;
  padding:12px 0 16px;
  background:linear-gradient(180deg,rgba(255,255,255,.72),#fff 28%);
  border-top:1px solid rgba(37,36,40,.08);
  box-shadow:0 -10px 24px rgba(37,36,40,.045);
}
.selected-plan-footer .pricing-plan-sign-up{margin-top:0;}
.selected-plan-footer .reassurance{margin-bottom:0;}
@media(max-width:900px){
  .selected-plan{max-height:calc(100vh - 32px);}
  .selected-plan-footer{padding-bottom:calc(16px + env(safe-area-inset-bottom));}
}
.selected-plan-body 
.selected-plan-body 
.selected-plan-body{display:flex;flex-direction:column;}
.selected-plan-body .selected-plan-top{order:1;}
.selected-plan-body .selected-plan-details{order:2;}
.selected-plan-body 
.selected-plan-body 
@media(max-width:900px){
  .selected-plan-body .selected-plan-top{order:1;}
  .selected-plan-body .selected-plan-details{order:2;}
  .selected-plan-body 
  .selected-plan-body 
}
.full-compare{max-width:1180px;margin:-28px auto 72px;padding:30px;border:1px solid var(--border);border-radius:28px;background:#fff;box-shadow:0 20px 70px rgba(37,36,40,.05)}
.full-compare-kicker{margin:0 0 8px;color:var(--muted);font-size:12px;font-weight:700;letter-spacing:.16em;text-transform:uppercase;text-align:center}
.full-compare h3{margin:0 0 22px;text-align:center;font-size:clamp(26px,3vw,40px);line-height:1.05;letter-spacing:-.04em;font-weight:700;color:var(--text)}
.compare-scroll{overflow-x:auto;-webkit-overflow-scrolling:touch;border:1px solid var(--border);border-radius:18px;background:#fff}
.compare-table{width:100%;min-width:860px;border-collapse:separate;border-spacing:0;font-size:13px}
.compare-table th,.compare-table td{padding:14px 14px;border-bottom:1px solid rgba(37,36,40,.08);border-right:1px solid rgba(37,36,40,.06);vertical-align:top;text-align:left}
.compare-table th:last-child,.compare-table td:last-child{border-right:0}
.compare-table tr:last-child td{border-bottom:0}
.compare-table thead th{position:sticky;top:0;background:#f8f8f9;z-index:1;font-size:12px;letter-spacing:.08em;text-transform:uppercase;color:#252428}
.compare-feature{width:22%;font-weight:700;color:#252428}
.compare-family td{background:#f3f3f4;color:#6f6d76;font-size:11px;font-weight:800;letter-spacing:.13em;text-transform:uppercase}
.compare-check{font-weight:700;color:#252428}
.compare-muted{color:var(--muted)}
@media(max-width:900px){.full-compare{margin:0 0 44px;padding:20px;border-radius:22px}.full-compare-kicker,.full-compare h3{text-align:left}.compare-table th,.compare-table td{padding:12px}.compare-table{font-size:12px}}
.selected-plan{
  height:calc(100vh - 48px);
  max-height:none;
}
.selected-plan-body{
  flex:1 1 auto;
  min-height:0;
  overflow-y:auto;
  overscroll-behavior:contain;
  scrollbar-gutter:stable;
}
.selected-plan-title{
  position:sticky;
  top:0;
  z-index:8;
  box-shadow:0 10px 22px rgba(37,36,40,.08);
}
.selected-plan-footer{
  position:relative;
  bottom:auto;
}
@media(max-width:900px){
  .selected-plan{
    height:calc(100svh - 32px);
    max-height:none;
  }
}
@media(min-width:1180px){
  .selected-plan{height:calc(100vh - 48px);}
}
.selected-plan-footer{
  padding:12px 22px 16px;
}
.selected-plan-footer-main{
  display:flex;
  align-items:center;
  gap:12px;
}
.footer-price{
  flex:0 0 auto;
  min-width:112px;
}
.footer-price-main{
  display:flex;
  align-items:flex-end;
  gap:3px;
}
.footer-price-value{
  font-size:31px;
  line-height:.9;
  font-weight:700;
  letter-spacing:-.055em;
  color:var(--text);
  white-space:nowrap;
}
.footer-price-suffix{
  margin-bottom:3px;
  color:var(--muted);
  font-size:12px;
  line-height:1.1;
  font-weight:700;
}
.footer-price-yearly{
  margin-top:3px;
  color:var(--muted);
  font-size:10.5px;
  line-height:1.2;
  font-weight:650;
  white-space:nowrap;
}
.selected-plan-footer .pricing-plan-sign-up{
  flex:1 1 auto;
  width:auto;
  min-width:0;
  margin:0;
}
.selected-plan-footer .reassurance{
  margin:9px 0 0;
}
@media(max-width:430px){
  .selected-plan-footer-main{align-items:stretch;flex-direction:column;gap:9px;}
  .footer-price{min-width:0;text-align:center;}
  .footer-price-main{justify-content:center;}
  .footer-price-value{font-size:34px;}
}
.selected-plan-body 
.selected-plan-body 
@media(max-width:900px){
  .selected-plan-body 
}
.selected-plan-details{padding-bottom:4px}
.selected-plan-body .settings,
.selected-plan-body .settings-warning{display:none!important}
.summary-icon img,
.option-icon{
  display:block;
  object-fit:contain;
}
.plan-diamond svg{
  display:block;
  width:21px;
  height:20px;
}
@media(min-width:901px){
  .selected-plan{
    height:calc(100vh - 48px);
  }
  .selected-plan-body{
    display:flex;
    flex-direction:column;
    min-height:0;
    overflow-y:auto;
    overscroll-behavior:contain;
    scroll-padding-top:64px;
  }
}
@media(max-width:900px){
  .selected-plan{
    height:auto;
    min-height:0;
    max-height:none;
  }
  .selected-plan-body{
    display:flex;
    flex-direction:column;
    overflow:visible;
  }
  .selected-plan-title{
    position:relative;
  }
  .selected-plan-footer{
    position:sticky;
    bottom:0;
    z-index:10;
  }
}
.pricing-plan{
  display:flex;
  align-items:center;
  justify-content:space-between;
  gap:24px;
  width:100%;
  text-align:left;
}
.pricing-plan-main{
  display:flex;
  flex-direction:column;
  align-items:flex-start;
  min-width:0;
  text-align:left;
}
.pricing-plan-name,
.pricing-plan-title,
.pricing-plan-lovedby{
  align-self:flex-start;
  text-align:left;
}
.pricing-plan-name{margin-bottom:8px}
.pricing-plan-lovedby{margin-top:10px}
.pricing-plan .right{
  display:flex;
  flex-direction:column;
  align-items:flex-end;
  justify-content:center;
  flex:0 0 auto;
  text-align:right;
}
.pricing-plan[data-plan-id="free"]{
  --plan-color:#e7e7e9!important;
  --plan-text:#252428!important;
}
.pricing-plan[data-plan-id="free"] .pricing-plan-name{
  background:#e7e7e9!important;
  color:#252428!important;
}
.selected-plan{
  display:flex;
  flex-direction:column;
  overflow:hidden;
  border-radius:18px;
}
.selected-plan.free-theme{
  --active:#e7e7e9!important;
  --active-rgb:231,231,233!important;
  --active-text:#252428!important;
}
.selected-plan-header{
  flex:0 0 auto;
  width:100%;
  box-sizing:border-box;
  padding:22px;
  border-radius:18px 18px 0 0;
  background:var(--active);
  color:var(--active-text);
  text-align:center;
}
.selected-plan-header .selected-plan-title,
.selected-plan-header .selected-plan-subtitle,
.selected-plan-header .selected-plan-loved{
  width:100%;
  margin-left:auto;
  margin-right:auto;
  text-align:center;
  color:inherit;
}
.selected-plan-header .selected-plan-title{
  margin-bottom:9px;
  box-shadow:none!important;
}
.selected-plan-header .selected-plan-loved{
  margin-top:8px;
  opacity:.76;
}
.selected-plan-body{
  flex:1 1 auto;
  min-height:0;
  overflow-y:auto;
  padding:14px 22px 12px;
  background:#fff;
}
.selected-plan-details{
  padding:0;
}
.recap-feature{
  padding:9px 0;
}
.checkout-stack{
  flex:0 0 auto;
  padding:14px 22px 16px;
  border-top:1px solid rgba(37,36,40,.08);
  background:#fff;
}
.checkout-size-tabs{
  display:grid;
  grid-template-columns:repeat(3,1fr);
  gap:6px;
  padding:4px;
  border:1px solid var(--border);
  border-radius:13px;
  background:#f4f4f5;
}
.checkout-size-tab{
  appearance:none;
  border:0;
  border-radius:9px;
  background:transparent;
  padding:9px 8px;
  color:var(--muted);
  cursor:pointer;
  font-size:12.5px;
  font-weight:700;
  line-height:1;
}
.checkout-size-tab:hover{color:var(--text)}
.checkout-size-tab[aria-selected="true"]{
  background:#fff;
  color:var(--text);
  box-shadow:0 1px 0 rgba(37,36,40,.08),0 2px 8px rgba(37,36,40,.06);
}
.checkout-size-tab:focus-visible{
  outline:3px solid rgba(var(--active-rgb),.25);
  outline-offset:2px;
}
.checkout-size-lines{
  margin-top:10px;
}
.checkout-size-line{
  display:flex;
  align-items:center;
  gap:9px;
  min-height:34px;
  padding:7px 0;
  color:var(--text);
  font-size:13px;
  line-height:1.25;
  font-weight:700;
}
.checkout-size-line + .checkout-size-line{
  border-top:1px solid rgba(37,36,40,.06);
}
.checkout-size-line img{
  width:18px;
  height:18px;
  flex:0 0 auto;
}
.checkout-bottom{
  display:flex;
  align-items:center;
  justify-content:space-between;
  gap:14px;
  margin-top:12px;
  padding-top:12px;
  border-top:1px solid rgba(37,36,40,.08);
}
.checkout-price{
  min-width:0;
}
.checkout-price-main{
  display:flex;
  align-items:flex-end;
  gap:3px;
}
.checkout-price-value{
  font-size:30px;
  line-height:.9;
  font-weight:700;
  letter-spacing:-.055em;
}
.checkout-price-suffix{
  color:var(--muted);
  font-size:11px;
  font-weight:650;
}
.checkout-price-yearly{
  margin-top:4px;
  color:var(--muted);
  font-size:10px;
  line-height:1.2;
  font-weight:600;
}
.checkout-cta{
  min-height:44px;
  padding:0 18px;
  border-radius:12px;
  white-space:nowrap;
}
.checkout-reassurance{
  margin-top:8px;
  color:var(--muted);
  font-size:10px;
  text-align:center;
}
@media(min-width:901px){
  .selected-plan{
    position:sticky;
    top:24px;
    height:calc(100vh - 48px);
    min-height:620px;
    max-height:860px;
  }
  .selected-plan-body{
    overscroll-behavior:contain;
  }
}
@media(max-width:900px){
  .selected-plan{
    position:relative;
    height:auto;
    min-height:0;
    max-height:none;
  }
  .selected-plan-body{
    overflow:visible;
  }
  .checkout-stack{
    position:sticky;
    bottom:0;
    z-index:10;
  }
}
@media(max-width:620px){
  .pricing-plan{
    flex-direction:column;
    align-items:flex-start;
    gap:12px;
  }
  .pricing-plan .right{
    align-items:flex-start;
    text-align:left;
  }
  .selected-plan-header{
    padding:18px 16px;
  }
  .selected-plan-body{
    padding:12px 16px 10px;
  }
  .checkout-stack{
    padding:12px 16px 14px;
  }
  .checkout-size-line{
    font-size:12.5px;
  }
  .checkout-bottom{
    gap:10px;
  }
  .checkout-price-value{
    font-size:27px;
  }
  .checkout-cta{
    padding:0 14px;
  }
}
.selected-plan{
  border-radius:18px;
  overflow:hidden;
  background:#fff;
}
.selected-plan-header{
  display:flex;
  align-items:center;
  justify-content:center;
  width:100%;
  min-height:42px;
  padding:10px 18px;
  box-sizing:border-box;
  border-radius:18px 18px 0 0;
  background:var(--active);
  color:var(--active-text);
  text-align:center;
  border-bottom:1px solid rgba(37,36,40,.05);
}
.selected-plan-header .selected-plan-title{
  display:inline-flex;
  align-items:center;
  justify-content:center;
  gap:7px;
  width:auto;
  max-width:100%;
  margin:0;
  padding:0;
  color:inherit;
  text-align:center;
  font-size:12px;
  line-height:1;
  font-weight:800;
  letter-spacing:.08em;
  text-transform:uppercase;
  white-space:normal;
  box-shadow:none!important;
  background:transparent!important;
  border:0!important;
}
.selected-plan-header .plan-diamond{
  display:inline-flex;
  align-items:center;
  justify-content:center;
  flex:0 0 auto;
}
.selected-plan-header .plan-diamond svg{
  width:17px;
  height:16px;
}
.selected-plan-intro{
  padding:18px 22px 10px;
  background:#fff;
  text-align:center;
}
.selected-plan-intro .selected-plan-subtitle{
  width:100%;
  max-width:440px;
  margin:0 auto;
  color:var(--text);
  text-align:center;
  font-size:17px;
  line-height:1.18;
  font-weight:700;
  letter-spacing:-.025em;
}
.selected-plan-intro .selected-plan-loved{
  width: